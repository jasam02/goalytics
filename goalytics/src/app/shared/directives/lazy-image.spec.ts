import { LazyImage } from './lazy-image';

describe('LazyImage', () => {
  it('should create an instance', () => {
    const directive = new LazyImage();
    expect(directive).toBeTruthy();
  });
});
